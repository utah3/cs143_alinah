unsigned char coin_types(unsigned int cents);
/*takes in a given number of cents and returns the fewest
number of coins you will need*/
unsigned short weeks_in_term(unsigned short total_mins,
unsigned short max_mins_per_lecture,
unsigned char max_lectures_week);
/*takes in a given number of minutes you want the class
to be and returns an extensive */
char month_ends_in(unsigned char month);
/*uses switch gives the last letter for the num month*/
unsigned int lit_segments(unsigned int number);
/*uses recursive function and two helpers*/

